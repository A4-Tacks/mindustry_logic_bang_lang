#**
* 这是0.12.0版本加入的语法, 可以在JumpCmp中加入这个比较式的依赖语句
* 它的优先级是溢出的, 大部分情况需要在括号内使用, 其优先级高于`||`
*
* 它为自身左右运算成员构建了一层作用域
*
* 在0.16.2版本中, 添加了一个语法糖,
* 可在箭头后面添加方括号进行正常的参数设置, 并且此时可以省略前面的代码体
*#

const X = ($ = 1 + 2;);

while ({take N = X;} => N > 10 && N < 30) {
    noop;
}
# 不用这个语法来解决这个问题的话, 总是不那么美观, 或者只能是看着X被take多次


# 利用参数设置语法糖
# 这里用到了cmper
const Cmper = goto(_0 > 0 && _0 < _1);
while (=>[a b] Cmper) {
    noop;
}
# 在v0.18.18这个设置参数的语法糖优先级将和"!"保持一致
